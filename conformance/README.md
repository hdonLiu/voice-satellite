# Conformance tests

Conformance tests replay valid and invalid golden traces against firmware,
Relay, and Connector protocol boundaries.

The suite will cover version negotiation, role isolation, state transitions,
message limits, cancellation, disconnects, duplicate requests, late events,
binary audio framing, and stable errors.
