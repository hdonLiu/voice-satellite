# Testkit package

The testkit will provide Fake Device, Relay, Connector, ASR, TTS, AgentRuntime,
and ACP process implementations. Contributors should be able to test most
changes without hardware, cloud credentials, or OpenClaw.
