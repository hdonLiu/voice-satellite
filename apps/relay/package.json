{
  "name": "@voice-satellite/relay",
  "version": "0.0.0",
  "private": true,
  "type": "module",
  "exports": {
    ".": "./src/index.ts"
  },
  "scripts": {
    "build": "tsc -p tsconfig.build.json",
    "start": "node dist/main.js",
    "check-config": "node dist/main.js --check-config",
    "typecheck": "tsc -p tsconfig.json",
    "test": "vitest run"
  },
  "dependencies": {
    "@voice-satellite/contracts": "workspace:*",
    "ws": "catalog:"
  }
}
